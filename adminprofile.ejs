<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Admin Panel</title>
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
  <style>
    body {
      font-family: Arial, sans-serif;
    }
    .admin-heading {
      background-color: #a7d8f7;
      padding: 20px;
      color: #fff;
      font-size: 24px;
      font-weight: bold;
      text-align: center;
      position: relative;
    }
    .sign-out {
      position: absolute;
      left: 10px;
      top: 50%;
      transform: translateY(-50%);
    }
  </style>
</head>
<body>

<div class="admin-heading">
  <a href="/" class="sign-out">
    <i class="fas fa-sign-out-alt"></i> Sign Out
  </a>
  <h1 class="grays" style="color: darkslategray; font-family: Impact, Haettenschweiler, 'Arial Narrow Bold', sans-serif;">Admin Panel</h1>
</div>

<div class="container">
    <br>
    <h2 style="font-family: Arial, Helvetica, sans-serif; font-weight: bolder;"> Users Record</h2>
    <br>
    <table class="table table-striped">
      <thead>
        <tr>
          <th>User Name</th>
          <th>Login Id</th>
          <th>Password</th>
          <th> Action </th>
        </tr>
      </thead>
      <% users.forEach((item)=> {%>
      <tbody>
        
        <tr>
          <td><%= item.fullname%></td>
          <td><%= item.loginname%></td>
          <td><%= item.password%></td>
          <td><a class="btn btn-primary" href="/deleteuser/<%= item.id %>" role="button">Delete User</a></td>

          <% })%>
        </tr>
      </tbody>
    </table>
  </div>

<script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.3/dist/umd/popper.min.js"></script>
<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>

</body>
</html>